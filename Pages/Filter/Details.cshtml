@page
@model Examensarbete.Pages.Filter.DetailsModel

@{
    ViewData["Title"] = "Details";
    ViewData["IsAdminSection"] = true;

}

<div class="mt-3 mb-3">
    <h2>Detaljerad översikt av Filter</h2>
</div>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-4">
            @Html.DisplayNameFor(model => model.Filter.Text)
        </dt>
        <dd class="col-sm-8">
            @Html.DisplayFor(model => model.Filter.Text)
        </dd>
    </dl>
</div>
<div class="d-flex align-items-center">
    <a class="btn btn-primary me-2" asp-page="./Edit" asp-route-id="@Model.Filter.Id">Ändra</a>
    <form method="post" asp-page-handler="Delete" asp-route-id="@Model.Filter.Id" onsubmit="return confirm('Är du säker på att du vill ta bort detta filter?')">
        <button class="btn btn-danger" type="submit">
            Ta bort
        </button>
    </form>
</div>
<div class="mt-3">
    <a asp-page="./Index">Back to List</a>
</div>

